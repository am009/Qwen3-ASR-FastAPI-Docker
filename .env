# --- HOST SETTINGS ---
API_PORT=18123
HOST_MODELS_PATH=./models

# --- STARTUP DEFAULTS ---
DEFAULT_MODEL=Qwen/Qwen3-ASR-1.8B
DEFAULT_DEVICE=cuda
DEFAULT_DTYPE=bf16
LOG_LEVEL=INFO

# --- CACHE (Must match volume mapping) ---
HF_HOME=/app/models
